# Use npm v8+ strict peer dependency resolution
legacy-peer-deps=false

# Ensure exact versions from lockfile
package-lock=true

# Audit and security settings
audit=true
audit-level=moderate

# Performance optimizations
prefer-offline=true
progress=true

# Engine strict mode
engine-strict=true
