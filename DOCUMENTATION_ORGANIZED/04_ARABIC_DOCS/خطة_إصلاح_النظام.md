# 🔧 خطة إصلاح شاملة: نظام البروفايل والاشتراكات

**تاريخ الخطة:** ديسمبر 2025  
**المشروع:** Bulgarian Car Marketplace (Globul Cars)  
**الحالة:** 🔴 حرجة - يتطلب إصلاح فوري

---

## 📊 ملخص الوضع الحالي

### المشاكل المكتشفة:
- ✅ **6 مشاكل حرجة** تؤثر على الإيرادات
- ⚠️ **المستخدمون يدفعون لكن لا يحصلون على الأذونات**
- ⚠️ **خسارة محتملة في الإيرادات**

### الأولوية:
1. 🔴 **حرجة فورية** - المشاكل #1 و #2 (تأثير مباشر على الإيرادات)
2. 🟠 **حرجة** - المشاكل #3, #4, #5, #6 (تأثير على الاستقرار)
3. 🟡 **تحسينات** - تحسينات إضافية

---

## 🎯 المرحلة 1: الإصلاحات الحرجة الفورية (اليوم)

### ✅ المهمة #1.1: إصلاح Stripe Webhook - تحديث `planTier`

**الأولوية:** 🔴 حرجة فورية  
**الوقت المتوقع:** 30 دقيقة  
**التأثير:** المستخدمون يحصلون على الأذونات بعد الدفع

#### الخطوات:

**1. تحديث `handleCheckoutCompleted` في `stripeWebhook.ts`:**

```typescript
// functions/src/subscriptions/stripeWebhook.ts
async function handleCheckoutCompleted(session: Stripe.Checkout.Session) {
  const userId = session.metadata?.firebaseUID;
  const planId = session.metadata?.planId;
  const planTier = session.metadata?.planTier;

  if (!userId || !planId) {
    logger.error('Missing metadata in checkout session', { sessionId: session.id });
    return;
  }

  try {
    const subscriptionId = session.subscription as string;
    const subscription = await stripe.subscriptions.retrieve(subscriptionId);
    const period = getSubscriptionPeriod(subscription);

    // ✅ FIX: تحديث planTier في root level أيضاً
    await db.collection('users').doc(userId).update({
      'planTier': planTier,  // ✅ إضافة هذا السطر
      'plan.tier': planTier,  // ✅ إضافة هذا أيضاً للتوافق
      'subscription.planId': planId,
      'subscription.planTier': planTier,
      'subscription.status': 'active',
      'subscription.stripeCustomerId': session.customer as string,
      'subscription.stripeSubscriptionId': subscriptionId,
      'subscription.stripePriceId': subscription.items.data[0].price.id,
      'subscription.currentPeriodStart': period.start ? new Date(period.start * 1000) : FieldValue.serverTimestamp(),
      'subscription.currentPeriodEnd': period.end ? new Date(period.end * 1000) : FieldValue.serverTimestamp(),
      'subscription.cancelAtPeriodEnd': false,
      updatedAt: FieldValue.serverTimestamp(),
    });

    logger.info('Subscription activated with planTier', { userId, planTier, subscriptionId });
  } catch (error: any) {
    logger.error('Error handling checkout completion', error);
    throw error;
  }
}
```

**2. تحديث `handleSubscriptionDeleted` في `stripeWebhook.ts`:**

```typescript
// functions/src/subscriptions/stripeWebhook.ts
async function handleSubscriptionDeleted(subscription: Stripe.Subscription) {
  // ... existing code ...

  // ✅ FIX: تحديث planTier إلى 'free' عند الإلغاء
  await db.collection('users').doc(userId).update({
    'planTier': 'free',  // ✅ إضافة هذا
    'plan.tier': 'free',  // ✅ إضافة هذا أيضاً
    'subscription.planId': 'free',
    'subscription.planTier': 'free',
    'subscription.status': 'canceled',
    'subscription.cancelAtPeriodEnd': false,
    updatedAt: FieldValue.serverTimestamp(),
  });

  // ... rest of code ...
}
```

**3. تحديث `cancelSubscription` في `cancelSubscription.ts`:**

```typescript
// functions/src/subscriptions/cancelSubscription.ts
if (immediate) {
  canceledSubscription = await stripe.subscriptions.cancel(subscriptionId);

  // ✅ FIX: تحديث planTier إلى 'free'
  await db.collection('users').doc(userId).update({
    'planTier': 'free',  // ✅ إضافة هذا
    'plan.tier': 'free',  // ✅ إضافة هذا أيضاً
    'subscription.planId': 'free',
    'subscription.planTier': 'free',
    'subscription.status': 'canceled',
    'subscription.cancelAtPeriodEnd': false,
    updatedAt: FieldValue.serverTimestamp(),
  });

  // ... rest of code ...
}
```

**4. تحديث `handlePaymentSucceeded` (اختياري - للتأكد):**

```typescript
// functions/src/subscriptions/stripeWebhook.ts
async function handlePaymentSucceeded(invoice: Stripe.Invoice) {
  // ... existing code ...

  // ✅ FIX: التأكد من أن planTier محدث
  const userData = usersSnapshot.docs[0].data();
  const currentPlanTier = userData.planTier || userData.subscription?.planTier || 'free';

  await db.collection('users').doc(userId).update({
    'planTier': currentPlanTier,  // ✅ التأكد من التحديث
    'subscription.status': 'active',
    'subscription.currentPeriodStart': period.start ? new Date(period.start * 1000) : FieldValue.serverTimestamp(),
    'subscription.currentPeriodEnd': period.end ? new Date(period.end * 1000) : FieldValue.serverTimestamp(),
    'subscription.cancelAtPeriodEnd': subscription.cancel_at_period_end,
    updatedAt: FieldValue.serverTimestamp(),
  });

  // ... rest of code ...
}
```

**الملفات المتأثرة:**
- ✅ `functions/src/subscriptions/stripeWebhook.ts` (3 مواقع)
- ✅ `functions/src/subscriptions/cancelSubscription.ts` (1 موقع)

**الاختبار:**
1. ✅ اختبار Stripe Webhook في Test Mode
2. ✅ التحقق من تحديث `planTier` في Firestore
3. ✅ التحقق من تحديث الأذونات في `ProfileTypeContext`

---

### ✅ المهمة #1.2: إصلاح تعريف الخطط في `getPermissions`

**الأولوية:** 🔴 حرجة فورية  
**الوقت المتوقع:** 20 دقيقة  
**التأثير:** الأذونات تُفعّل بشكل صحيح للمشتركين

#### الخطوات:

**تحديث `getPermissions` في `ProfileTypeContext.tsx`:**

```typescript
// bulgarian-car-marketplace/src/contexts/ProfileTypeContext.tsx

// ✅ FIX: تحديث getPermissions لتطابق الخطط الحالية
function getPermissions(profileType: ProfileType, planTier: PlanTier): ProfilePermissions {
  // ✅ FIX: استخدام الخطط الحالية فقط (free, dealer, company)
  const PLAN_LIMITS: Record<PlanTier, number> = {
    free: 5,
    dealer: 15,
    company: -1  // unlimited
  };

  const maxListings = PLAN_LIMITS[planTier] || 5;

  // Base permissions for all
  const base: ProfilePermissions = {
    canAddListings: true,
    maxListings,
    hasAnalytics: false,
    hasAdvancedAnalytics: false,
    hasTeam: false,
    canExportData: false,
    hasPrioritySupport: false,
    canUseQuickReplies: false,
    canBulkEdit: false,
    canImportCSV: false,
    canUseAPI: false
  };

  // ✅ FIX: إزالة التحقق من premium (غير موجود)
  // ❌ REMOVED: if (planTier === 'premium') { ... }

  // ✅ FIX: Dealer plan (planTier === 'dealer')
  if (profileType === 'dealer' && planTier === 'dealer') {
    return {
      ...base,
      hasAnalytics: true,
      canUseQuickReplies: true,
      canBulkEdit: true,
      hasPrioritySupport: true
      // ✅ FIX: إزالة التحقق من dealer_pro/dealer_enterprise (غير موجودة)
    };
  }

  // ✅ FIX: Company plan (planTier === 'company')
  if (profileType === 'company' && planTier === 'company') {
    return {
      ...base,
      hasAnalytics: true,
      hasAdvancedAnalytics: true,
      hasTeam: true,
      canExportData: true,
      canUseQuickReplies: true,
      canBulkEdit: true,
      canImportCSV: true,
      canUseAPI: true,
      hasPrioritySupport: true
      // ✅ FIX: إزالة التحقق من company_starter (غير موجود)
    };
  }

  // Free plan (default)
  return base;
}
```

**الملفات المتأثرة:**
- ✅ `bulgarian-car-marketplace/src/contexts/ProfileTypeContext.tsx`

**الاختبار:**
1. ✅ اختبار الأذونات للخطة المجانية
2. ✅ اختبار الأذونات لخطة Dealer
3. ✅ اختبار الأذونات لخطة Company
4. ✅ التحقق من `maxListings` لكل خطة

---

## 🎯 المرحلة 2: الإصلاحات الحرجة (هذا الأسبوع)

### ✅ المهمة #2.1: توحيد تعريف `PlanTier` Type

**الأولوية:** 🟠 حرجة  
**الوقت المتوقع:** 15 دقيقة  
**التأثير:** منع أخطاء TypeScript وعدم التطابق

#### الخطوات:

**1. إنشاء ملف مشترك للأنواع:**

```typescript
// bulgarian-car-marketplace/src/types/plan.types.ts
// ✅ المصدر الوحيد لتعريف PlanTier

export type PlanTier = 'free' | 'dealer' | 'company';

export type BillingInterval = 'monthly' | 'annual';

export type PlanStatus = 'active' | 'trial' | 'past_due' | 'canceled';

// ✅ Export للاستخدام في جميع الملفات
```

**2. تحديث `bulgarian-user.types.ts`:**

```typescript
// bulgarian-car-marketplace/src/types/user/bulgarian-user.types.ts

// ✅ FIX: استيراد من الملف المشترك
import type { PlanTier, BillingInterval } from '../plan.types';

// ❌ REMOVE: export type PlanTier = 'free' | 'dealer' | 'company';
// ❌ REMOVE: export type BillingInterval = 'monthly' | 'annual';

// ✅ استخدام الأنواع المستوردة
export interface BaseProfile {
  // ...
  planTier: PlanTier;  // ✅ من plan.types.ts
  // ...
}
```

**3. تحديث `billing/types.ts`:**

```typescript
// bulgarian-car-marketplace/src/features/billing/types.ts

// ✅ FIX: استيراد من الملف المشترك
import type { PlanTier, BillingInterval, PlanStatus } from '../../types/plan.types';

// ❌ REMOVE: export type PlanTier = 'free' | 'dealer' | 'company';
// ❌ REMOVE: export type BillingInterval = 'monthly' | 'annual';
// ❌ REMOVE: export type PlanStatus = 'active' | 'trial' | 'past_due' | 'canceled';

// ✅ استخدام الأنواع المستوردة
export interface Plan {
  id: PlanTier;  // ✅ من plan.types.ts
  // ...
}

export interface Subscription {
  planId: PlanTier;  // ✅ من plan.types.ts
  status: PlanStatus;  // ✅ من plan.types.ts
  interval: BillingInterval;  // ✅ من plan.types.ts
  // ...
}
```

**4. تحديث جميع الاستيرادات:**

```typescript
// ✅ في جميع الملفات التي تستورد PlanTier
// استبدال:
// import type { PlanTier } from '../features/billing/types';
// أو
// import type { PlanTier } from '../types/user/bulgarian-user.types';

// بـ:
import type { PlanTier } from '../types/plan.types';
```

**الملفات المتأثرة:**
- ✅ `bulgarian-car-marketplace/src/types/user/bulgarian-user.types.ts`
- ✅ `bulgarian-car-marketplace/src/features/billing/types.ts`
- ✅ `bulgarian-car-marketplace/src/contexts/ProfileTypeContext.tsx`
- ✅ جميع الملفات التي تستورد `PlanTier`

**الاختبار:**
1. ✅ التحقق من عدم وجود أخطاء TypeScript
2. ✅ التحقق من أن جميع الاستيرادات تعمل
3. ✅ اختبار التطبيق للتأكد من عدم كسر أي شيء

---

### ✅ المهمة #2.2: توحيد `PLAN_LIMITS` في ملف مشترك

**الأولوية:** 🟠 حرجة  
**الوقت المتوقع:** 20 دقيقة  
**التأثير:** منع التكرار وضمان التطابق

#### الخطوات:

**1. إنشاء ملف مشترك للحدود:**

```typescript
// bulgarian-car-marketplace/src/constants/plan-limits.ts
// ✅ المصدر الوحيد لتعريف حدود الخطط

import type { PlanTier } from '../types/plan.types';

/**
 * Plan Limits Configuration
 * Updated: December 2025 - Simplified to 3 plans
 * 
 * Limits:
 * - free: 5 cars/month
 * - dealer: 15 cars/month
 * - company: unlimited (-1)
 */
export const PLAN_LIMITS: Record<PlanTier, number> = {
  free: 5,
  dealer: 15,
  company: -1  // unlimited
};

/**
 * Get plan limit for a specific tier
 * @param planTier - Plan tier
 * @returns number - -1 for unlimited
 */
export function getPlanLimit(planTier: PlanTier): number {
  return PLAN_LIMITS[planTier] || 5;
}

/**
 * Check if plan has unlimited listings
 * @param planTier - Plan tier
 * @returns boolean
 */
export function isUnlimitedPlan(planTier: PlanTier): boolean {
  return PLAN_LIMITS[planTier] === -1;
}
```

**2. تحديث `ProfileTypeContext.tsx`:**

```typescript
// bulgarian-car-marketplace/src/contexts/ProfileTypeContext.tsx

// ✅ FIX: استيراد من الملف المشترك
import { PLAN_LIMITS } from '../constants/plan-limits';

// ❌ REMOVE: const PLAN_LIMITS: Record<PlanTier, number> = { ... };

function getPermissions(profileType: ProfileType, planTier: PlanTier): ProfilePermissions {
  // ✅ استخدام PLAN_LIMITS المستورد
  const maxListings = PLAN_LIMITS[planTier] || 5;
  
  // ... rest of code ...
}
```

**3. تحديث `listing-limits.ts`:**

```typescript
// bulgarian-car-marketplace/src/utils/listing-limits.ts

// ✅ FIX: استيراد من الملف المشترك
import { PLAN_LIMITS, getPlanLimit as getPlanLimitFromConstants } from '../constants/plan-limits';

// ❌ REMOVE: const PLAN_LIMITS: Record<PlanTier, number> = { ... };

export async function canAddListing(userId: string): Promise<boolean> {
  // ... existing code ...
  
  const planTier = userData.plan?.tier || userData.planTier || 'free';
  const limit = PLAN_LIMITS[planTier as PlanTier] || 3;  // ✅ استخدام المستورد
  
  // ... rest of code ...
}

export async function getRemainingListings(userId: string): Promise<number> {
  // ... existing code ...
  
  const planTier = userData.plan?.tier || userData.planTier || 'free';
  const limit = PLAN_LIMITS[planTier as PlanTier] || 3;  // ✅ استخدام المستورد
  
  // ... rest of code ...
}

// ✅ FIX: استخدام الدالة من الملف المشترك
export function getPlanLimit(planTier: PlanTier): number {
  return getPlanLimitFromConstants(planTier);
}
```

**الملفات المتأثرة:**
- ✅ `bulgarian-car-marketplace/src/contexts/ProfileTypeContext.tsx`
- ✅ `bulgarian-car-marketplace/src/utils/listing-limits.ts`

**الاختبار:**
1. ✅ التحقق من أن الحدود متطابقة في كلا الملفين
2. ✅ اختبار `canAddListing()` و `getRemainingListings()`
3. ✅ اختبار الأذونات في `ProfileTypeContext`

---

### ✅ المهمة #2.3: توحيد البنية - استخدام `planTier` root level فقط

**الأولوية:** 🟠 حرجة  
**الوقت المتوقع:** 30 دقيقة  
**التأثير:** بنية واضحة وموحدة

#### الخطوات:

**1. تحديث `ProfileTypeContext.tsx` لقراءة `planTier` فقط:**

```typescript
// bulgarian-car-marketplace/src/contexts/ProfileTypeContext.tsx

// ✅ FIX: قراءة planTier من root level فقط
const loadProfileType = async () => {
  // ... existing code ...
  
  if (userDoc.exists()) {
    const userData = userDoc.data();
    
    // ✅ FIX: قراءة planTier من root level فقط
    const type = userData.profileType || 'private';
    const tier = userData.planTier || 'free';  // ✅ من root level فقط
    
    setProfileType(type);
    setPlanTier(tier);
  }
  
  // ... rest of code ...
};

// ✅ FIX: في onSnapshot أيضاً
unsubscribe = onSnapshot(
  doc(db, 'users', currentUser.uid),
  (userDoc) => {
    if (userDoc.exists()) {
      const userData = userDoc.data();
      
      const type = userData.profileType || 'private';
      const tier = userData.planTier || 'free';  // ✅ من root level فقط
      
      setProfileType(type);
      setPlanTier(tier);
    }
  }
);
```

**2. تحديث `listing-limits.ts`:**

```typescript
// bulgarian-car-marketplace/src/utils/listing-limits.ts

export async function canAddListing(userId: string): Promise<boolean> {
  // ... existing code ...
  
  const userData = userDoc.data();
  // ✅ FIX: قراءة planTier من root level فقط
  const planTier = userData.planTier || 'free';  // ✅ من root level
  const limit = PLAN_LIMITS[planTier as PlanTier] || 3;
  
  // ... rest of code ...
}
```

**3. تحديث `BillingService.ts`:**

```typescript
// bulgarian-car-marketplace/src/features/billing/BillingService.ts

async getCurrentSubscription(userId: string): Promise<Subscription | null> {
  // ... existing code ...
  
  const userData = userDoc.data();
  const plan = userData.plan;
  
  // ✅ FIX: قراءة planTier من root level
  const planTier = userData.planTier || plan?.tier || 'free';
  
  return {
    userId,
    planId: planTier,  // ✅ استخدام planTier من root level
    status: plan?.status || 'active',
    // ... rest of code ...
  };
}
```

**4. إزالة `plan.tier` من `bulgarian-user.types.ts` (اختياري - للتوافق):**

```typescript
// bulgarian-car-marketplace/src/types/user/bulgarian-user.types.ts

// ✅ يمكن الاحتفاظ بـ plan.tier للتوافق مع الكود القديم
// لكن planTier root level هو المصدر الأساسي
```

**الملفات المتأثرة:**
- ✅ `bulgarian-car-marketplace/src/contexts/ProfileTypeContext.tsx`
- ✅ `bulgarian-car-marketplace/src/utils/listing-limits.ts`
- ✅ `bulgarian-car-marketplace/src/features/billing/BillingService.ts`
- ✅ جميع الملفات التي تقرأ `plan.tier`

**الاختبار:**
1. ✅ التحقق من أن `planTier` يُقرأ بشكل صحيح
2. ✅ اختبار التطبيق بعد التغييرات
3. ✅ التحقق من عدم كسر أي وظائف موجودة

---

## 🎯 المرحلة 3: تحسينات ودمج الخدمات (هذا الأسبوع)

### ✅ المهمة #3.1: دمج أو ربط BillingService و SubscriptionService

**الأولوية:** 🟡 متوسطة  
**الوقت المتوقع:** 45 دقيقة  
**التأثير:** تقليل التكرار وتحسين البنية

#### الخيار 1: دمج SubscriptionService في BillingService (موصى به)

**الخطوات:**

**1. تحديث `BillingService.ts` لاستخدام `SubscriptionService`:**

```typescript
// bulgarian-car-marketplace/src/features/billing/BillingService.ts

import { subscriptionService } from '../../services/subscription/SubscriptionService';

class BillingService {
  // ✅ FIX: استخدام SubscriptionService للعمليات
  async createCheckoutSession(
    userId: string,
    planId: PlanTier,
    interval: BillingInterval
  ): Promise<{ url: string; sessionId: string }> {
    try {
      // ✅ استخدام SubscriptionService
      const result = await subscriptionService.createCheckoutSession({
        userId,
        planId,
        successUrl: `${window.location.origin}/billing/success`,
        cancelUrl: `${window.location.origin}/billing/canceled`,
      });

      if (result.success && result.checkoutUrl) {
        return {
          url: result.checkoutUrl,
          sessionId: result.sessionId || '',
        };
      } else {
        throw new Error(result.error || 'Failed to create checkout session');
      }
    } catch (error: any) {
      logger.error('Error creating checkout session', error);
      throw error;
    }
  }

  async cancelSubscription(userId: string): Promise<void> {
    try {
      // ✅ استخدام SubscriptionService
      const result = await subscriptionService.cancelSubscription({
        userId,
        immediate: false,
      });

      if (!result.success) {
        throw new Error(result.error || 'Failed to cancel subscription');
      }

      logger.info('Subscription canceled for user:', userId);
    } catch (error) {
      logger.error('Error canceling subscription:', error);
      throw error;
    }
  }

  // ... rest of methods ...
}
```

**2. حذف `SubscriptionService` إذا لم يُستخدم في مكان آخر:**

```bash
# ✅ حذف الملف إذا لم يُستخدم
rm bulgarian-car-marketplace/src/services/subscription/SubscriptionService.ts
```

**الملفات المتأثرة:**
- ✅ `bulgarian-car-marketplace/src/features/billing/BillingService.ts`
- ✅ `bulgarian-car-marketplace/src/services/subscription/SubscriptionService.ts` (حذف)

**الاختبار:**
1. ✅ اختبار إنشاء جلسة checkout
2. ✅ اختبار إلغاء الاشتراك
3. ✅ التحقق من أن جميع المكونات تعمل

---

### ✅ المهمة #3.2: استخدام ProfileTypeContext للأذونات في صفحات الإعلانات

**الأولوية:** 🟡 متوسطة  
**الوقت المتوقع:** 30 دقيقة  
**التأثير:** مزامنة فورية للأذونات

#### الخطوات:

**1. تحديث صفحات إضافة الإعلانات:**

```typescript
// bulgarian-car-marketplace/src/pages/04_car-selling/sell/VehicleStartPageUnified.tsx

import { useProfileType } from '../../../contexts/ProfileTypeContext';

function VehicleStartPageUnified() {
  const { permissions, profileType } = useProfileType();
  
  // ✅ FIX: استخدام permissions من Context بدلاً من canAddListing()
  const canAdd = permissions.canAddListings && 
                 (permissions.maxListings === -1 || 
                  activeListings < permissions.maxListings);
  
  if (!canAdd) {
    // Show limit message
    const maxListings = permissions.maxListings;
    // ... show error ...
  }
  
  // ... rest of code ...
}
```

**2. تحديث `listing-limits.ts` لاستخدام Context (اختياري):**

```typescript
// ✅ يمكن الاحتفاظ بـ canAddListing() للاستخدام في Server-side
// لكن Frontend يجب أن يستخدم Context
```

**الملفات المتأثرة:**
- ✅ `bulgarian-car-marketplace/src/pages/04_car-selling/sell/VehicleStartPageUnified.tsx`
- ✅ `bulgarian-car-marketplace/src/pages/04_car-selling/sell/VehicleStartPageNew.tsx`
- ✅ جميع صفحات إضافة الإعلانات

**الاختبار:**
1. ✅ اختبار إضافة إعلان عند الوصول للحد
2. ✅ اختبار الرسائل التحذيرية
3. ✅ اختبار التحديث الفوري للأذونات

---

## 📋 جدول التنفيذ المقترح

### اليوم (اليوم):
- ✅ **المهمة #1.1** - إصلاح Stripe Webhook (30 دقيقة)
- ✅ **المهمة #1.2** - إصلاح تعريف الخطط (20 دقيقة)
- ✅ **الاختبار** - اختبار شامل (30 دقيقة)

**المجموع:** ~1.5 ساعة

### هذا الأسبوع:
- ✅ **المهمة #2.1** - توحيد PlanTier Type (15 دقيقة)
- ✅ **المهمة #2.2** - توحيد PLAN_LIMITS (20 دقيقة)
- ✅ **المهمة #2.3** - توحيد البنية (30 دقيقة)
- ✅ **المهمة #3.1** - دمج الخدمات (45 دقيقة)
- ✅ **المهمة #3.2** - استخدام Context (30 دقيقة)
- ✅ **الاختبار الشامل** (1 ساعة)

**المجموع:** ~3.5 ساعة

---

## ✅ قائمة التحقق (Checklist)

### المرحلة 1 (اليوم):
- [ ] تحديث `handleCheckoutCompleted` في `stripeWebhook.ts`
- [ ] تحديث `handleSubscriptionDeleted` في `stripeWebhook.ts`
- [ ] تحديث `cancelSubscription` في `cancelSubscription.ts`
- [ ] تحديث `getPermissions` في `ProfileTypeContext.tsx`
- [ ] اختبار Stripe Webhook في Test Mode
- [ ] اختبار الأذونات بعد الاشتراك
- [ ] التحقق من تحديث `planTier` في Firestore

### المرحلة 2 (هذا الأسبوع):
- [ ] إنشاء `src/types/plan.types.ts`
- [ ] تحديث جميع الاستيرادات لـ `PlanTier`
- [ ] إنشاء `src/constants/plan-limits.ts`
- [ ] تحديث `ProfileTypeContext.tsx` لاستخدام `PLAN_LIMITS` المشترك
- [ ] تحديث `listing-limits.ts` لاستخدام `PLAN_LIMITS` المشترك
- [ ] تحديث `ProfileTypeContext.tsx` لقراءة `planTier` فقط
- [ ] تحديث `listing-limits.ts` لقراءة `planTier` فقط
- [ ] تحديث `BillingService.ts` لقراءة `planTier` فقط
- [ ] اختبار TypeScript compilation
- [ ] اختبار التطبيق الشامل

### المرحلة 3 (هذا الأسبوع):
- [ ] تحديث `BillingService.ts` لاستخدام `SubscriptionService`
- [ ] حذف `SubscriptionService` إذا لم يُستخدم
- [ ] تحديث صفحات إضافة الإعلانات لاستخدام Context
- [ ] اختبار إضافة الإعلانات
- [ ] اختبار الرسائل التحذيرية

---

## 🧪 خطة الاختبار

### اختبارات الوحدة:
1. ✅ اختبار `getPermissions()` مع جميع الخطط
2. ✅ اختبار `canAddListing()` مع جميع الخطط
3. ✅ اختبار `getPlanLimit()` مع جميع الخطط

### اختبارات التكامل:
1. ✅ اختبار Stripe Webhook في Test Mode
2. ✅ اختبار الاشتراك الكامل (Checkout → Webhook → Firestore → Context)
3. ✅ اختبار إلغاء الاشتراك
4. ✅ اختبار تحديث الأذونات في الوقت الفعلي

### اختبارات End-to-End:
1. ✅ المستخدم يشترك في خطة Dealer
2. ✅ التحقق من تحديث `planTier` في Firestore
3. ✅ التحقق من تحديث الأذونات في `ProfileTypeContext`
4. ✅ التحقق من إمكانية إضافة 15 إعلان
5. ✅ التحقق من عدم إمكانية إضافة أكثر من 15 إعلان

---

## 🚨 المخاطر والتحذيرات

### مخاطر عالية:
1. ⚠️ **تغييرات في Stripe Webhook** - قد تؤثر على الاشتراكات الحالية
   - **الحل:** اختبار شامل في Test Mode قبل Production

2. ⚠️ **تغييرات في Type Definitions** - قد تكسر الكود الموجود
   - **الحل:** تحديث جميع الاستيرادات تدريجياً

### مخاطر متوسطة:
3. ⚠️ **تغييرات في البنية** - قد تؤثر على الكود القديم
   - **الحل:** الاحتفاظ بـ `plan.tier` للتوافق مع الكود القديم

---

## 📊 مقاييس النجاح

### بعد الإصلاحات:
- ✅ **100%** من المشتركين يحصلون على الأذونات فوراً
- ✅ **0** أخطاء TypeScript
- ✅ **0** تكرار في الكود
- ✅ **< 1 ثانية** وقت تحديث الأذونات بعد الاشتراك

### قبل الإصلاحات:
- ❌ **0%** من المشتركين يحصلون على الأذونات (المشكلة الحالية)
- ⚠️ **2** تعريفات لـ `PlanTier`
- ⚠️ **2** تعريفات لـ `PLAN_LIMITS`
- ⚠️ **~5 ثواني** وقت تحديث الأذونات (بعد إعادة تحميل الصفحة)

---

## 🎯 الخلاصة

### الأولوية الفورية:
1. 🔴 **إصلاح Stripe Webhook** - اليوم
2. 🔴 **إصلاح تعريف الخطط** - اليوم

### هذا الأسبوع:
3. 🟠 **توحيد الأنواع والحدود** - هذا الأسبوع
4. 🟡 **تحسينات إضافية** - هذا الأسبوع

### النتيجة المتوقعة:
- ✅ نظام متكامل 100%
- ✅ لا تكرار في الكود
- ✅ بنية واضحة وموحدة
- ✅ جميع المشتركين يحصلون على الأذونات فوراً

---

**تم إعداد الخطة بواسطة:** Auto (Cursor AI)  
**التاريخ:** ديسمبر 2025  
**الإصدار:** 1.0  
**الحالة:** ✅ جاهزة للتنفيذ

