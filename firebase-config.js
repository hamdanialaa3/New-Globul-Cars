// firebase-config.js - Simplified mock for testing
// In production, this would be the compiled version of firebase-config.ts

export const functions = {
  // Mock Firebase Functions instance
};

export const BULGARIAN_CONFIG = {
  locale: 'bg-BG',
  currency: 'EUR',
  timezone: 'Europe/Sofia',
  region: 'Bulgaria',
  phonePrefix: '+359',
  defaultLanguage: 'bg',
  supportedLanguages: ['bg', 'en']
};