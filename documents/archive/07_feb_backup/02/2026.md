# KOLI ONE — دليل التنفيذ الشامل (Execution Manual)

> **ما هذا الملف؟** تعليمات تنفيذية دقيقة لإصلاح وتطوير مشروع Koli One.
> يقرأه أي نموذج ذكاء اصطناعي وينفذ المهمة مباشرة بدون تخمين.
>
> **تاريخ الإعداد:** 7 فبراير 2026
>
> **ترتيب التنفيذ:** نفّذ المهام بالترتيب الرقمي (TASK-01 أولاً، ثم TASK-02... إلخ). لا تقفز.

---

## القسم 0: سياق المشروع (اقرأ قبل أي شيء)

### 0.1 — ما هو Koli One؟

| المعلومة | القيمة |
|---|---|
| النوع | سوق إلكتروني لبيع وشراء السيارات (Car Marketplace) |
| السوق المستهدف | بلغاريا 🇧🇬 |
| اللغات | البلغارية (أساسية) + الإنجليزية (ثانوية) |
| العملة | EUR (يورو) |
| الموقع الحي | https://koli.one |
| المنافسون | mobile.bg, cars.bg, auto.bg |

### 0.2 — التقنيات (Tech Stack)

| التطبيق | المسار من جذر المشروع | التقنيات |
|---|---|---|
| **الويب (Web)** | `web/` | React 18 + TypeScript (strict) + Styled-Components + Firebase 12 + Algolia + CRACO/Webpack 5 (react-scripts 5.0.1) |
| **الموبايل (Mobile)** | `mobile_new/` | Expo 54 + React Native 0.81.5 + React 19.1 + expo-router 6 + styled-components 6 + Firebase 11.3 |
| **الباك إند** | Firebase (مشروع: `fire-new-globul`) | Firestore + Realtime Database + Auth + Storage + Hosting + Cloud Functions |
| **البحث** | Algolia (ويب فقط حالياً) | فهرس `cars` — بحث أقل من 50ms |

### 0.3 — المجلدات المهمة (خريطة الملفات)

```
mobile_new/
├── app/                          ← الراوتر (expo-router)
│   ├── (tabs)/
│   │   ├── index.tsx             ← الصفحة الرئيسية (HOME PAGE)
│   │   ├── search.tsx            ← صفحة البحث
│   │   ├── sell.tsx              ← صفحة البيع
│   │   ├── messages.tsx          ← صفحة الرسائل
│   │   └── profile.tsx           ← صفحة الملف الشخصي
│   └── _layout.tsx               ← Root Layout + Providers
├── src/
│   ├── components/
│   │   ├── common/               ← مكوّنات مشتركة
│   │   └── home/                 ← مكوّنات الصفحة الرئيسية
│   ├── hooks/
│   │   └── useMobileSearch.ts    ← هوك البحث (يستخدم Firestore حالياً - بطيء)
│   ├── services/
│   │   ├── firebase.ts           ← إعداد Firebase
│   │   ├── ListingService.ts     ← جلب الإعلانات
│   │   ├── MessagingService.ts   ← الرسائل
│   │   ├── SellService.ts        ← نشر إعلان
│   │   ├── userService.ts        ← خدمة المستخدم
│   │   ├── NotificationService.ts
│   │   ├── SubscriptionService.ts
│   │   ├── PlatformSyncService.ts
│   │   ├── OfficialPublisherService.ts
│   │   ├── numeric-car-system.service.ts
│   │   ├── numeric-id-counter.service.ts
│   │   ├── numeric-id-lookup.service.ts
│   │   ├── AnalyticsService.ts
│   │   ├── ConversationService.ts
│   │   ├── ai/ai.service.ts
│   │   └── search/               ← مجلد البحث (حالياً لا يحتوي على Algolia)
│   └── styles/
│       └── theme.ts              ← ألوان وخطوط التطبيق
├── package.json
└── tsconfig.json

web/
├── src/
│   ├── services/
│   │   ├── logger-service.ts         ← نظام الـ logging (446 سطر - مرجع للنسخ)
│   │   └── search/
│   │       └── algolia-search.service.ts  ← Algolia (360 سطر - مرجع للنسخ)
│   └── locales/
│       ├── index.ts                  ← تصدير الترجمات { bg, en }
│       ├── useTranslation.ts         ← هوك الترجمة (يستخدم localStorage)
│       ├── bg/                       ← 26 ملف ترجمة بلغارية
│       │   ├── index.ts
│       │   ├── home.ts, cars.ts, sell.ts, nav.ts, header.ts
│       │   ├── common.ts, auth.ts, errors.ts, settings.ts
│       │   ├── messages.ts, messaging.ts, profile.ts, profileTypes.ts
│       │   ├── footer.ts, help.ts, contact.ts, billing.ts
│       │   ├── dealer.ts, bodyTypes.ts, savedSearches.ts
│       │   ├── advancedSearch.ts, ai.ts, cookiePolicy.ts
│       │   └── mapPage.ts, myListings.ts
│       └── en/                       ← 26 ملف ترجمة إنجليزية (نفس الهيكل)
```

### 0.4 — قوانين ممنوع كسرها (NEVER BREAK)

| # | القاعدة | التفصيل |
|---|---|---|
| 1 | **ممنوع `console.log/error/warn` في الكود** | استخدم `logger` فقط. الويب يحظره عبر `web/scripts/ban-console.js` |
| 2 | **ممنوع كشف Firebase UID في الروابط** | استخدم Numeric IDs فقط. انظر `numeric-id-*.ts` |
| 3 | **ممنوع حذف ملفات الويب** | انقلها إلى `web/DDD/` بدلاً من الحذف |
| 4 | **Firestore listeners تحتاج `isActive` guard** | لتجنب تحديث الـ state بعد unmount |
| 5 | **المركبات مقسمة على 7 مجموعات Firestore** | `cars`, `passenger_cars`, `suvs`, `vans`, `motorcycles`, `trucks`, `buses` |
| 6 | **الويب يستخدم path aliases `@/...`** | مُعَّرفة في `web/tsconfig.json`. لا تستخدم مسارات نسبية عميقة في الويب |
| 7 | **اللغة الافتراضية = `bg` (بلغاري)** | ليس `en`. هذا تطبيق للسوق البلغاري |

### 0.5 — الحزم المثبتة مسبقاً في الموبايل (لا تثبتها مرة ثانية)

```
@react-native-async-storage/async-storage: 2.2.0   ← موجود
expo-notifications: ^0.32.16                        ← موجود
expo-camera: ~17.0.10                               ← موجود
expo-image-picker: ~17.0.10                         ← موجود
expo-location: ~19.0.8                              ← موجود
firebase: ^11.3.0                                   ← موجود
styled-components: ^6.3.8                           ← موجود
zod: ^4.3.6                                         ← موجود
```

---

## القسم 1: تشخيص الوضع الحالي

### 1.1 — مشاكل الموبايل

| المشكلة | التفصيل | الأثر |
|---|---|---|
| البحث مزيف | `useMobileSearch.ts` يستخدم Firestore مباشرة، يجلب `limit(20)` فقط per collection، يفلترها في الذاكرة. لا Algolia. | "BMW X5 Sofia" = صفر نتائج أو 10 ثوان |
| لا ترجمة بلغارية | التطبيق كامل English-only hardcoded. الويب فيه 26 ملف BG + 26 ملف EN. | تطبيق للسوق البلغاري بدون بلغارية = فشل |
| Home Page فوضى | 16 section في ScrollView بدون lazy loading. مكوّنات: HeroSection, SmartHeroRecommendations, AIAnalysisBanner, VisualSearchTeaser, VehicleClassifications, LifeMomentsBrowse, FeaturedShowcase, PopularBrands, MostDemandedCategories, UnifiedSmartSell, DealersSpotlight, TrustAndStats, StayConnected, LoyaltyBanner, RecentBrowsingSection | UX سيء، بطء |
| console.log في 12 ملف | 30 موقع في `services/` يستخدم `console.log/error/warn`. لا يوجد `logger-service.ts` | يسرّب معلومات في production |
| هوية بصرية مختلفة | الويب = `#003366` أزرق. الموبايل = `#FF7900` برتقالي + `#7B61FF` بنفسجي | يبدو كتطبيقين مختلفين |

### 1.2 — مشاكل الويب

| المشكلة | التفصيل |
|---|---|
| كود ميت | 250+ component، كثير منها demo/unused (IoT, 3D, DigitalTwin, BundleAnalyzer) |
| خدمات مكررة | 200+ service، تكرار: favorites x2, image x5, firebase x8, autonomous-resale x6 |
| CRA deprecated | `react-scripts 5.0.1` + CRACO/Webpack 5. Cold start = 60-90 ثانية |

### 1.3 — ما يعمل جيداً (لا تلمسه ولا تعدّله)

| الميزة | الحالة |
|---|---|
| Sell Workflow (Web) | نظام 7 خطوات + 7 collections + numeric IDs — ممتاز |
| Messaging (Web) | RTDB real-time + typing indicators + read receipts — متقدم |
| Algolia Search (Web) | sub-50ms + typo tolerance + faceted search — يعمل |
| Localization (Web) | BG/EN كاملة، 26 ملف لكل لغة — ممتاز |
| Profile System | 3-tier (Private/Dealer/Corporate) + trust score — مميز |
| Bulgaria Map | Leaflet + real car counts per city — ميزة تنافسية |
| Security Headers | CSP, X-Frame-Options, HSTS — محترم |

---

## القسم 2: جدول المهام الرئيسي

**نفّذ بالترتيب. TASK-01 أولاً.**

| المهمة | الاسم | الأثر | الجهد | الأولوية | الصعوبة |
|---|---|---|---|---|---|
| **TASK-01** | حذف console.log + إنشاء logger-service.ts للموبايل | stability | 2 ساعة | 🔴 P0 | ⚡ سهل |
| **TASK-02** | تبسيط Home Page الموبايل (16 → 5 sections) | UX أفضل 5x | 4 ساعات | 🔴 P0 | ⚡ سهل |
| **TASK-03** | توحيد الهوية البصرية (ألوان Theme) | brand recognition | 4 ساعات | 🔴 P0 | ⚡ سهل |
| **TASK-04** | إضافة Algolia للموبايل | بحث حقيقي | 8 ساعات | 🔴 P0 | 🟡 متوسط |
| **TASK-05** | إضافة الترجمة البلغارية للموبايل | بدونها لا مستخدمين | 16 ساعة | 🔴 P0 | 🟡 متوسط |
| **TASK-06** | Quick Replies + Offer في Messaging | conversion | 8 ساعات | 🟠 P1 | 🟡 متوسط |
| **TASK-07** | Price Drop Alerts + Push Notifications | retention | 16 ساعة | 🟠 P1 | 🟡 متوسط |
| **TASK-08** | Reviews للموبايل | trust | 12 ساعة | 🟠 P1 | 🟡 متوسط |
| **TASK-09** | AI Price Estimator (السعر العادل) | ميزة قاتلة | 20 ساعة | 🟠 P1 | 🔴 صعب |
| **TASK-10** | Onboarding Flow للتطبيق | first impression | 8 ساعات | 🟡 P2 | 🟡 متوسط |
| **TASK-11** | حذف Dead Components من الويب | code health | 4 ساعات | 🟡 P2 | ⚡ سهل |
| **TASK-12** | VIN Check | trust | 8 ساعات | 🟡 P2 | 🟡 متوسط |
| **TASK-13** | SEO Pages بالبلغارية | organic traffic | 16 ساعة | 🟡 P2 | 🔴 صعب |
| **TASK-14** | Smart Sell from Photo | wow factor | 12 ساعة | 🟡 P2 | 🔴 صعب |
| **TASK-15** | Migrate CRA → Vite (الويب) | build speed | 20 ساعة | 🟡 P2 | 🔴 صعب |
| **TASK-16** | Dealer Dashboard اشتراك | revenue | 40 ساعة | 🟢 P3 | 🔴 صعب |
| **TASK-17** | Financing Calculator | conversion | 16 ساعة | 🟢 P3 | 🔴 صعب |

---

## القسم 3: تعليمات تنفيذ كل مهمة

---

### TASK-01: حذف console.log + إنشاء logger-service.ts للموبايل

**الصعوبة:** ⚡ سهل — أي نموذج يكفي
**المتطلبات:** file edit + terminal access
**عدد الملفات المتأثرة:** 13 ملف (1 جديد + 12 تعديل)

---

#### الخطوة 1 من 3: أنشئ ملف جديد

**المسار الدقيق:** `mobile_new/src/services/logger-service.ts`

**المحتوى الكامل:**

```typescript
/**
 * Logger Service for Mobile (Koli One)
 * بديل لـ console.log/error/warn
 * في production يكتم الـ debug/info
 */

type LogLevel = 'debug' | 'info' | 'warn' | 'error';

interface LogContext {
  [key: string]: unknown;
}

const IS_DEV = __DEV__; // Expo/React Native يوفر __DEV__ تلقائياً

class Logger {
  private log(level: LogLevel, message: string, error?: Error | unknown, context?: LogContext) {
    if (!IS_DEV && (level === 'debug' || level === 'info')) return;

    const timestamp = new Date().toISOString();
    const prefix = `[${level.toUpperCase()}] [${timestamp}]`;

    switch (level) {
      case 'debug':
      case 'info':
        if (IS_DEV) console.log(prefix, message, context || '');
        break;
      case 'warn':
        if (IS_DEV) console.warn(prefix, message, context || '');
        break;
      case 'error':
        console.error(prefix, message, error || '', context || '');
        break;
    }
  }

  debug(message: string, context?: LogContext) {
    this.log('debug', message, undefined, context);
  }

  info(message: string, context?: LogContext) {
    this.log('info', message, undefined, context);
  }

  warn(message: string, context?: LogContext) {
    this.log('warn', message, undefined, context);
  }

  error(message: string, error?: Error | unknown, context?: LogContext) {
    this.log('error', message, error, context);
  }
}

export const logger = new Logger();
export default logger;
```

---

#### الخطوة 2 من 3: عدّل كل ملف يحتوي console.log/error/warn

**القاعدة العامة:**
1. أضف هذا السطر في أعلى الملف (بعد آخر import):
   - إذا الملف في `mobile_new/src/services/`: `import { logger } from './logger-service';`
   - إذا الملف في `mobile_new/src/services/ai/`: `import { logger } from '../logger-service';`
2. استبدل كل `console.log(...)` بـ `logger.info(...)`
3. استبدل كل `console.error(...)` بـ `logger.error(...)`
4. استبدل كل `console.warn(...)` بـ `logger.warn(...)`

**القائمة الكاملة — 12 ملف، 30 موقع:**

**ملف 1: `mobile_new/src/services/firebase.ts`**
- سطر 65: `console.log(...)` → `logger.info(...)`
- سطر 67: `console.error(...)` → `logger.error(...)`
- import path: `import { logger } from './logger-service';`

**ملف 2: `mobile_new/src/services/ai/ai.service.ts`**
- سطر 42: `console.error('AI Service Error:', error)` → `logger.error('AI Service Error', error)`
- سطر 75: `console.error('AI Vision Error:', error)` → `logger.error('AI Vision Error', error)`
- import path: `import { logger } from '../logger-service';`

**ملف 3: `mobile_new/src/services/userService.ts`**
- سطر 14: `console.error(...)` → `logger.error(...)`
- سطر 33: `console.error(...)` → `logger.error(...)`
- سطر 62: `console.error(...)` → `logger.error(...)`
- سطر 130: `console.error(...)` → `logger.error(...)`
- سطر 142: `console.error(...)` → `logger.error(...)`
- import path: `import { logger } from './logger-service';`

**ملف 4: `mobile_new/src/services/numeric-id-counter.service.ts`**
- سطر 35: `console.log(...)` → `logger.info(...)`
- سطر 38: `console.error(...)` → `logger.error(...)`
- import path: `import { logger } from './logger-service';`

**ملف 5: `mobile_new/src/services/SellService.ts`**
- سطر 18: `console.log(...)` → `logger.info(...)`
- سطر 37: `console.log(...)` → `logger.info(...)`
- سطر 41: `console.error(...)` → `logger.error(...)`
- سطر 65: `console.error(...)` → `logger.error(...)`
- import path: `import { logger } from './logger-service';`

**ملف 6: `mobile_new/src/services/SubscriptionService.ts`**
- سطر 102: `console.error(...)` → `logger.error(...)`
- import path: `import { logger } from './logger-service';`

**ملف 7: `mobile_new/src/services/PlatformSyncService.ts`**
- سطر 137: `console.error(...)` → `logger.error(...)`
- سطر 173: `console.warn(...)` → `logger.warn(...)`
- import path: `import { logger } from './logger-service';`

**ملف 8: `mobile_new/src/services/OfficialPublisherService.ts`**
- سطر 21: `console.error(...)` → `logger.error(...)`
- import path: `import { logger } from './logger-service';`

**ملف 9: `mobile_new/src/services/numeric-id-lookup.service.ts`**
- سطر 26: `console.warn(...)` → `logger.warn(...)`
- سطر 36: `console.error(...)` → `logger.error(...)`
- سطر 57: `console.warn(...)` → `logger.warn(...)`
- سطر 63: `console.error(...)` → `logger.error(...)`
- سطر 126: `console.error(...)` → `logger.error(...)`
- import path: `import { logger } from './logger-service';`

**ملف 10: `mobile_new/src/services/numeric-car-system.service.ts`**
- سطر 111: `console.log(...)` → `logger.info(...)`
- سطر 120: `console.error(...)` → `logger.error(...)`
- import path: `import { logger } from './logger-service';`

**ملف 11: `mobile_new/src/services/NotificationService.ts`**
- سطر 35: `console.warn(...)` → `logger.warn(...)`
- سطر 46: `console.log(...)` → `logger.info(...)`
- سطر 48: `console.warn(...)` → `logger.warn(...)`
- import path: `import { logger } from './logger-service';`

**ملف 12: `mobile_new/src/services/ListingService.ts`**
- سطر 38: `console.warn(...)` → `logger.warn(...)`
- سطر 164: `console.error(...)` → `logger.error(...)`
- import path: `import { logger } from './logger-service';`

---

#### الخطوة 3 من 3: تحقق من النتيجة

شغّل هذا الأمر:
```bash
cd mobile_new && grep -rn "console\.\(log\|error\|warn\)" src/services/ --include="*.ts" --include="*.tsx"
```

**النتيجة المتوقعة:** صفر نتائج (0 matches).
**إذا وجدت نتائج:** أصلحها بنفس النمط أعلاه.

**استثناء واحد مسموح:** داخل ملف `logger-service.ts` نفسه يُسمح باستخدام `console.log/error/warn` — هذا متوقع لأنه الملف الذي يغلّف هذه الدوال.

---

### TASK-02: تبسيط Home Page الموبايل (16 → 5 sections)

**الصعوبة:** ⚡ سهل — أي نموذج يكفي
**عدد الملفات المتأثرة:** 1 ملف فقط
**الملف:** `mobile_new/app/(tabs)/index.tsx`

---

#### ماذا نريد بالضبط؟

**الحالي (16 مكوّن):**
```
HeroSection, SmartHeroRecommendations, AIAnalysisBanner, VisualSearchTeaser,
VehicleClassifications, LifeMomentsBrowse, FeaturedShowcase, PopularBrands,
MostDemandedCategories, UnifiedSmartSell, DealersSpotlight, TrustAndStats,
StayConnected, LoyaltyBanner, RecentBrowsingSection
```

**المطلوب (5 مكوّنات فقط):**
```
HeroSection, FeaturedShowcase, RecentBrowsingSection, PopularBrands, TrustAndStats
```

**مكوّنات تُحذف من الصفحة فقط (لا تحذف ملفاتها):**
```
SmartHeroRecommendations, AIAnalysisBanner, VisualSearchTeaser,
VehicleClassifications, LifeMomentsBrowse, MostDemandedCategories,
UnifiedSmartSell, DealersSpotlight, StayConnected, LoyaltyBanner
```

---

#### التعديل

استبدل **كل** محتوى `mobile_new/app/(tabs)/index.tsx` بهذا:

```tsx
// app/(tabs)/index.tsx
import React from 'react';
import styled from 'styled-components/native';
import { StatusBar } from 'expo-status-bar';
import { theme } from '../../src/styles/theme';
import { MobileHeader } from '../../src/components/common/MobileHeader';

// 5 مكوّنات فقط — الأهم أولاً
import HeroSection from '../../src/components/home/HeroSection';
import FeaturedShowcase from '../../src/components/home/FeaturedShowcase';
import RecentBrowsingSection from '../../src/components/home/RecentBrowsingSection';
import PopularBrands from '../../src/components/home/PopularBrands';
import TrustAndStats from '../../src/components/home/TrustAndStats';

const Container = styled.ScrollView`
  flex: 1;
  background-color: ${(props: any) => props.theme?.colors?.background?.dark || '#0f172a'};
`;

const SectionSpacer = styled.View`
  height: 24px;
`;

export default function HomeScreen() {
  return (
    <Container theme={theme} showsVerticalScrollIndicator={false}>
      <StatusBar style="light" />
      <MobileHeader transparent dark />

      {/* 1. Hero — بحث + فلاتر سريعة */}
      <HeroSection />
      <SectionSpacer />

      {/* 2. آخر السيارات المضافة */}
      <FeaturedShowcase />
      <SectionSpacer />

      {/* 3. تصفحت مؤخراً */}
      <RecentBrowsingSection />
      <SectionSpacer />

      {/* 4. ماركات شائعة */}
      <PopularBrands />
      <SectionSpacer />

      {/* 5. إحصائيات وثقة */}
      <TrustAndStats />

      {/* Bottom Padding */}
      <SectionSpacer style={{ height: 100 }} />
    </Container>
  );
}
```

#### تحقق

```bash
cd mobile_new && npx tsc --noEmit
```
يجب ألا تظهر أخطاء متعلقة بـ `index.tsx`.

---

### TASK-03: توحيد الهوية البصرية (ألوان Theme)

**الصعوبة:** ⚡ سهل — أي نموذج يكفي
**عدد الملفات المتأثرة:** 1 ملف فقط
**الملف:** `mobile_new/src/styles/theme.ts`

---

#### القرار المعماري

الموبايل يتبنى ألوان الويب:
- **Primary = أزرق داكن `#003366`** (مثل الويب)
- **Secondary = برتقالي `#FF7900`** (accent للأزرار فقط)
- **Accent = أخضر تقني `#00D4AA`** (للحالات الإيجابية)

---

#### جدول الاستبدال الدقيق

كل تعديل في ملف `mobile_new/src/styles/theme.ts`:

| الخاصية | القيمة القديمة | القيمة الجديدة |
|---|---|---|
| `primary.main` | `'#FF7900'` | `'#003366'` |
| `primary.light` | `'#FF9433'` | `'#004C99'` |
| `primary.dark` | `'#E56D00'` | `'#002244'` |
| `secondary.main` | `'#00D4AA'` | `'#FF7900'` |
| `secondary.light` | `'#33DDBB'` | `'#FF9433'` |
| `secondary.dark` | `'#00AA88'` | `'#E56D00'` |
| `accent.main` | `'#7B61FF'` | `'#00D4AA'` |
| `accent.light` | `'#9580FF'` | `'#33DDBB'` |
| `accent.dark` | `'#624ECC'` | `'#00AA88'` |
| `border.focus` | `'#FF7900'` | `'#003366'` |
| `status.info` | `'#7B61FF'` | `'#003366'` |

#### ⚠️ لا تغيّر هذه القيم

- `status.success` (`'#00D4AA'`) — ابقِه
- `status.warning` (`'#FFC107'`) — ابقِه
- `status.error` (`'#CC0000'`) — ابقِه
- `background.*` — ابقِها كما هي
- `text.*` — ابقِها كما هي
- `brand.*` — ابقِها كما هي
- كتلة `glassmorphism` — ابقِها كما هي
- كتلة `typography` — ابقِها كما هي

#### تحقق

```bash
cd mobile_new && npx expo start
```
افتح التطبيق وتأكد أن الألوان تغيرت من برتقالي إلى أزرق داكن.

---

### TASK-04: إضافة Algolia للموبايل

**الصعوبة:** 🟡 متوسط
**المرجع:** `web/src/services/search/algolia-search.service.ts` (360 سطر — اقرأه كمرجع)
**عدد الملفات المتأثرة:** 2 جديدة + 1 تعديل

---

#### الخطوة 1 من 4: تثبيت الحزمة

```bash
cd mobile_new
npx expo install algoliasearch
```

---

#### الخطوة 2 من 4: أنشئ ملف Algolia Service

**المسار:** `mobile_new/src/services/search/algolia-search.service.ts`

```typescript
/**
 * Algolia Search Service for Mobile (Koli One)
 * نسخة من نمط الويب: web/src/services/search/algolia-search.service.ts
 */
import algoliasearch from 'algoliasearch';
import { logger } from '../logger-service';

// ⚠️ مفاتيح Algolia — يجب تعبئتها من نفس مفاتيح الويب
// للحصول عليها شغّل: grep -r "ALGOLIA" web/.env* web/src/config/
const ALGOLIA_APP_ID = 'YOUR_ALGOLIA_APP_ID';         // ← استبدل بالقيمة الحقيقية
const ALGOLIA_SEARCH_KEY = 'YOUR_ALGOLIA_SEARCH_KEY';  // ← استبدل بالقيمة الحقيقية
const ALGOLIA_INDEX_NAME = 'cars';

export interface AlgoliaCarHit {
  objectID: string;
  make?: string;
  model?: string;
  year?: number;
  price?: number;
  mileage?: number;
  city?: string;
  images?: string[];
  vehicleType?: string;
  [key: string]: unknown;
}

export interface AlgoliaSearchResult {
  hits: AlgoliaCarHit[];
  totalHits: number;
  processingTimeMs: number;
  page: number;
  totalPages: number;
}

class AlgoliaSearchService {
  private static instance: AlgoliaSearchService;
  private client: ReturnType<typeof algoliasearch> | null = null;
  private index: any = null;
  private isInitialized = false;

  private constructor() {
    if (ALGOLIA_APP_ID !== 'YOUR_ALGOLIA_APP_ID' && ALGOLIA_SEARCH_KEY !== 'YOUR_ALGOLIA_SEARCH_KEY') {
      try {
        this.client = algoliasearch(ALGOLIA_APP_ID, ALGOLIA_SEARCH_KEY);
        this.index = this.client.initIndex(ALGOLIA_INDEX_NAME);
        this.isInitialized = true;
        logger.info('Algolia initialized for mobile', { indexName: ALGOLIA_INDEX_NAME });
      } catch (error) {
        logger.error('Failed to initialize Algolia', error);
      }
    } else {
      logger.warn('Algolia not configured — still using placeholder keys. Replace ALGOLIA_APP_ID and ALGOLIA_SEARCH_KEY.');
    }
  }

  static getInstance(): AlgoliaSearchService {
    if (!AlgoliaSearchService.instance) {
      AlgoliaSearchService.instance = new AlgoliaSearchService();
    }
    return AlgoliaSearchService.instance;
  }

  async search(
    query: string,
    options?: {
      filters?: string;
      facetFilters?: string[][];
      hitsPerPage?: number;
      page?: number;
    }
  ): Promise<AlgoliaSearchResult> {
    if (!this.isInitialized || !this.index) {
      logger.warn('Algolia not initialized, returning empty results');
      return { hits: [], totalHits: 0, processingTimeMs: 0, page: 0, totalPages: 0 };
    }

    try {
      const result = await this.index.search(query, {
        hitsPerPage: options?.hitsPerPage || 20,
        page: options?.page || 0,
        filters: options?.filters,
        facetFilters: options?.facetFilters,
      });

      return {
        hits: result.hits as AlgoliaCarHit[],
        totalHits: result.nbHits,
        processingTimeMs: result.processingTimeMS,
        page: result.page,
        totalPages: result.nbPages,
      };
    } catch (error) {
      logger.error('Algolia search failed', error, { query });
      return { hits: [], totalHits: 0, processingTimeMs: 0, page: 0, totalPages: 0 };
    }
  }
}

export const algoliaSearch = AlgoliaSearchService.getInstance();
export default algoliaSearch;
```

---

#### الخطوة 3 من 4: حدّث useMobileSearch.ts

**الملف:** `mobile_new/src/hooks/useMobileSearch.ts`

**المطلوب:**
1. أضف `import algoliaSearch from '../services/search/algolia-search.service';` في الأعلى
2. استبدل استدعاء `ListingService.search()` بـ `algoliaSearch.search(query)`
3. حوّل `AlgoliaCarHit[]` إلى الشكل المتوقع في الـ UI

**تنبيه:** اقرأ `useMobileSearch.ts` أولاً لفهم الشكل الحالي، ثم عدّله.

---

#### الخطوة 4 من 4: الحصول على مفاتيح Algolia

```bash
grep -r "ALGOLIA" web/.env* web/src/config/ --include="*.ts" --include="*.env*" --include="*.local"
```

المفاتيح يجب أن تكون **نفسها** للويب والموبايل (نفس فهرس `cars`).
استبدل `YOUR_ALGOLIA_APP_ID` و `YOUR_ALGOLIA_SEARCH_KEY` بالقيم الحقيقية.

---

### TASK-05: إضافة الترجمة البلغارية للموبايل (i18n)

**الصعوبة:** 🟡 متوسط
**المرجع:** `web/src/locales/` (انسخ النظام بالكامل)
**عدد الملفات الجديدة:** ~20 ملف

---

#### الخطوة 1 من 4: أنشئ هيكل المجلدات

```
mobile_new/src/locales/
├── index.ts
├── useTranslation.ts
├── LanguageProvider.tsx
├── bg/
│   ├── index.ts
│   ├── common.ts
│   ├── home.ts
│   ├── cars.ts
│   ├── sell.ts
│   ├── messages.ts
│   ├── profile.ts
│   ├── nav.ts
│   └── auth.ts
└── en/
    ├── index.ts
    ├── common.ts
    ├── home.ts
    ├── cars.ts
    ├── sell.ts
    ├── messages.ts
    ├── profile.ts
    ├── nav.ts
    └── auth.ts
```

---

#### الخطوة 2 من 4: أنشئ index.ts و useTranslation.ts

**ملف `mobile_new/src/locales/index.ts`:**
```typescript
import * as bg from './bg';
import * as en from './en';

export const translations = { bg, en } as const;
export type Language = 'bg' | 'en';
export default translations;
```

**ملف `mobile_new/src/locales/useTranslation.ts`:**
```typescript
import { useState, useEffect, useCallback } from 'react';
import AsyncStorage from '@react-native-async-storage/async-storage';
import translations from './index';

export type Language = 'bg' | 'en';
const STORAGE_KEY = 'app_language';

export function useTranslation() {
  const [language, setLang] = useState<Language>('bg'); // افتراضي: بلغاري

  useEffect(() => {
    AsyncStorage.getItem(STORAGE_KEY).then((stored) => {
      if (stored === 'bg' || stored === 'en') setLang(stored);
    });
  }, []);

  const setLanguage = useCallback(async (lang: Language) => {
    setLang(lang);
    await AsyncStorage.setItem(STORAGE_KEY, lang);
  }, []);

  const t = useCallback(
    (namespace: string, key: string): string => {
      try {
        const ns = (translations as any)[language]?.[namespace];
        return ns?.[key] || key;
      } catch {
        return key;
      }
    },
    [language]
  );

  return { language, setLanguage, t };
}
```

---

#### الخطوة 3 من 4: انسخ ملفات الترجمة من الويب

**لكل ملف من هذه الملفات:**
```
web/src/locales/bg/common.ts  →  mobile_new/src/locales/bg/common.ts
web/src/locales/bg/home.ts    →  mobile_new/src/locales/bg/home.ts
web/src/locales/bg/cars.ts    →  mobile_new/src/locales/bg/cars.ts
web/src/locales/bg/sell.ts    →  mobile_new/src/locales/bg/sell.ts
web/src/locales/bg/messages.ts → mobile_new/src/locales/bg/messages.ts
web/src/locales/bg/profile.ts →  mobile_new/src/locales/bg/profile.ts
web/src/locales/bg/nav.ts     →  mobile_new/src/locales/bg/nav.ts
web/src/locales/bg/auth.ts    →  mobile_new/src/locales/bg/auth.ts
```
**وكذلك مجلد `en/` بنفس الطريقة.**

اقرأ كل ملف من الويب وانسخ محتواه. **لا تغيّر المفاتيح أو القيم.**

**ملف `mobile_new/src/locales/bg/index.ts`:**
```typescript
export { common } from './common';
export { home } from './home';
export { cars } from './cars';
export { sell } from './sell';
export { messages } from './messages';
export { profile } from './profile';
export { nav } from './nav';
export { auth } from './auth';
```

---

#### الخطوة 4 من 4: أنشئ LanguageProvider ولف التطبيق

**ملف `mobile_new/src/locales/LanguageProvider.tsx`:**
```tsx
import React, { createContext, useContext } from 'react';
import { useTranslation, Language } from './useTranslation';

interface LanguageContextType {
  language: Language;
  setLanguage: (lang: Language) => Promise<void>;
  t: (namespace: string, key: string) => string;
}

const LanguageContext = createContext<LanguageContextType | null>(null);

export function LanguageProvider({ children }: { children: React.ReactNode }) {
  const translation = useTranslation();
  return (
    <LanguageContext.Provider value={translation}>
      {children}
    </LanguageContext.Provider>
  );
}

export function useLanguage(): LanguageContextType {
  const ctx = useContext(LanguageContext);
  if (!ctx) throw new Error('useLanguage must be used within LanguageProvider');
  return ctx;
}
```

**ثم في `mobile_new/app/_layout.tsx`:**
أضف `import { LanguageProvider } from '../src/locales/LanguageProvider';`
ولف محتوى التطبيق بـ `<LanguageProvider>...</LanguageProvider>`

#### ⚠️ تحذيرات

- **اللغة الافتراضية = `bg`** (بلغاري)، ليس `en`
- `AsyncStorage` مثبت بالفعل (`@react-native-async-storage/async-storage: 2.2.0`) — لا تثبته مرة ثانية
- **لا تستخدم `react-i18next`** — نظام خفيف مخصص أفضل (مثل نظام الويب بالضبط)

---

### TASK-06: Quick Replies + Offer في Messaging

**الصعوبة:** 🟡 متوسط
**المرجع:** الويب فيه Offer Bubble — ابحث عنه بـ `grep -rn "offer\|OfferBubble" web/src/components/ --include="*.tsx" -l`

---

#### المطلوب

**1. Quick Reply Buttons** — 3 أزرار ثابتة أعلى حقل الإدخال في شاشة المحادثة:

| الزر (BG) | الزر (EN) |
|---|---|
| Колата налична ли е? | Is the car available? |
| Каква е най-ниската цена? | What's the lowest price? |
| Къде мога да я видя? | Where can I see it? |

عند الضغط → يُرسل النص مباشرة كرسالة عبر `MessagingService.ts`

**2. Offer Button** — زر "عرض سعر" يفتح حقل رقمي → يُرسل كرسالة خاصة بتصميم مميز (فقاعة عرض سعر)

#### الملفات الجديدة

1. `mobile_new/src/components/messaging/QuickReplies.tsx`
2. `mobile_new/src/components/messaging/OfferBubble.tsx`

---

### TASK-07: Price Drop Alerts + Push Notifications

**الصعوبة:** 🟡 متوسط
**المتطلبات:** Cloud Functions + expo-notifications (موجود)

---

#### المطلوب

1. **Cloud Function** تراقب تغيّر حقل `price` في أي إعلان في الـ 7 مجموعات
2. عند `newPrice < oldPrice` → تفحص مجموعة `saved_searches` → ترسل Push notification للمطابقات
3. **في الموبايل:** شاشة "تنبيهاتي" تعرض آخر التنبيهات
4. **المرجع:** `mobile_new/src/services/NotificationService.ts` + `expo-notifications`

---

### TASK-08: Reviews للموبايل

**الصعوبة:** 🟡 متوسط
**المرجع:** الويب فيه Review system كامل

---

#### المطلوب

1. ابحث عن مكوّنات الـ Reviews في الويب:
   ```bash
   grep -rn "review\|Review" web/src/components/ --include="*.tsx" -l
   grep -rn "review\|Review" web/src/services/ --include="*.ts" -l
   ```
2. أنشئ مكوّنين:
   - `mobile_new/src/components/reviews/ReviewsList.tsx` — يعرض التقييمات
   - `mobile_new/src/components/reviews/ReviewComposer.tsx` — نموذج كتابة تقييم (نجوم + نص)
3. اربطهما مع Firestore collection `reviews`

---

### TASK-09: AI Price Estimator (السعر العادل)

**الصعوبة:** 🔴 صعب — يحتاج نموذج قوي أو مطور بشري
**⚠️ لا تنفذها بدون تخطيط مسبق. اقرأ كل التعليمات أولاً.**

---

#### الفكرة

المستخدم يدخل: `BMW X5 2019 120,000km`
النظام يرد: **"السعر العادل: €22,000 - €26,000"**

**لا أحد في بلغاريا يقدم هذه الميزة. هذه هي الميزة القاتلة.**

#### خطة التنفيذ

1. **Data Layer:** جمع أسعار السيارات المشابهة من Firestore (نفس make + model + year ± 1)
2. **Statistical Model:** حساب median + percentile 25 + percentile 75
3. **Service:** `mobile_new/src/services/PriceEstimatorService.ts`
   - Input: `{ make, model, year, mileage }`
   - Output: `{ min, fair, max, sampleSize }`
4. **UI:** مكوّن يعرض النتيجة (مثل مقياس حرارة — أخضر/أصفر/أحمر)
5. **تحسين لاحق:** استخدام Algolia facets بدل Firestore queries

---

### TASK-10: Onboarding Flow للتطبيق

**الصعوبة:** 🟡 متوسط

---

#### المطلوب

3 شاشات onboarding تظهر **أول مرة فقط:**

| الشاشة | المحتوى |
|---|---|
| 1 | "ما الذي تبحث عنه؟" → أشتري / أبيع / أتصفح |
| 2 | "ما نوع السيارة؟" → 3 خيارات بصور (سيارة / SUV / دراجة) |
| 3 | "ما مدينتك؟" → dropdown لمدن بلغاريا |

→ ثم مباشرة إلى نتائج مخصصة.

#### التنفيذ

1. مجلد: `mobile_new/src/components/onboarding/`
2. حالة "تم العرض": `AsyncStorage.getItem('onboarding_completed')`
3. في `_layout.tsx` أو `index.tsx`: إذا `!onboarding_completed` → اعرض Onboarding

---

### TASK-11: حذف Dead Components من الويب

**الصعوبة:** ⚡ سهل — أي نموذج يكفي
**⚠️ القاعدة: لا تحذف الملفات. انقلها إلى `web/DDD/`**

---

#### الملفات المطلوب نقلها

| الاسم | السبب |
|---|---|
| كل ملف يحتوي `CarCarousel3D` | لا أحد يحتاج 3D carousel |
| كل ملف يحتوي `MechanicalGear3D` | ليس مطلوباً |
| كل ملف يحتوي `DigitalTwinDashboard` | ليس IoT platform |
| كل ملف يحتوي `CarIoTStatus` | ليس IoT platform |
| كل ملف يحتوي `IoTStatusWidget` | ليس IoT platform |
| كل ملف يحتوي `BundleAnalyzer` | أداة dev وليست مكوّن UI |
| كل ملف يحتوي `FullThemeDemo` | Demo فقط |
| كل ملف يحتوي `ThemePreview` | Demo فقط |
| `web/src/services/autonomous-resale-*.ts` (6 ملفات) | ميزة لم تُطلق |
| `web/src/services/n8n-integration.ts` | لا يوجد n8n server |
| `web/src/services/socket-service.ts` | لا يوجد WebSocket server |

#### الخطوات

```bash
cd web
mkdir -p DDD/dead-components DDD/dead-services

# 1. ابحث عن الملفات:
find src/components -name "*CarCarousel3D*" -o -name "*MechanicalGear3D*" -o -name "*DigitalTwin*" -o -name "*CarIoTStatus*" -o -name "*IoTStatusWidget*" -o -name "*BundleAnalyzer*" -o -name "*FullThemeDemo*" -o -name "*ThemePreview*"

# 2. انقل كل ملف وجدته إلى DDD/dead-components/
# مثال: mv src/components/xxx/CarCarousel3D.tsx DDD/dead-components/

# 3. انقل الخدمات الميتة:
# mv src/services/autonomous-resale-*.ts DDD/dead-services/
# mv src/services/n8n-integration.ts DDD/dead-services/
# mv src/services/socket-service.ts DDD/dead-services/

# 4. تحقق أن البناء يعمل:
npm run build
```

**إذا ظهرت أخطاء import بعد النقل:** أزل سطر الـ `import` من الملف الذي يستورد المكوّن المنقول.

---

### TASK-12 إلى TASK-17: مهام متقدمة (ملخص مختصر)

هذه المهام **تحتاج تخطيطاً تفصيلياً قبل التنفيذ**:

---

| المهمة | الفكرة | المتطلب الرئيسي |
|---|---|---|
| **TASK-12: VIN Check** | حقل VIN → API يرجع عدد ملاك + حوادث | API: NHTSA (مجاني للأمريكية) + EuVin (أوروبية) |
| **TASK-13: SEO Pages** | صفحات `/bmw-sofia`, `/audi-plovdiv` | SSR/SSG — CRA لا يدعم، يحتاج TASK-15 أولاً |
| **TASK-14: Smart Sell from Photo** | تصوير سيارة → AI يملأ الفورم | ربط `SmartCamera.tsx` + Gemini API + Form autofill |
| **TASK-15: CRA → Vite** | cold start: 90s → 2s | `@vitejs/plugin-react` + نقل CRACO config. **خطر عالي.** |
| **TASK-16: Dealer Dashboard** | اشتراك مدفوع + analytics + auto-republish | نظام دفع كامل — مصدر الربح الرئيسي |
| **TASK-17: Financing Calculator** | "350 лв/شهر لـ 5 سنوات" | API بنوك (UniCredit Bulbank, DSK Bank) أو حساب تقريبي |

---

## القسم 4: تصنيف المهام حسب مستوى النموذج المطلوب

### ⚡ أي نموذج AI يكفي (سهل — تنفيذي مباشر)

| المهمة | السبب |
|---|---|
| TASK-01: Logger Service | find & replace + ملف template واحد. كل سطر محدد أعلاه. |
| TASK-02: تبسيط Home Page | حذف imports من ملف واحد. الكود الجديد جاهز كاملاً. |
| TASK-03: توحيد الألوان | تغيير قيم في ملف واحد. الجدول يحدد كل قيمة بالضبط. |
| TASK-11: حذف Dead Components | نقل ملفات. لا يحتاج فهم عميق. |

### 🟡 يحتاج نموذج متوسط (Sonnet / GPT-4o / Gemini Pro)

| المهمة | السبب |
|---|---|
| TASK-04: Algolia للموبايل | نسخ pattern من الويب. الكود جاهز 90% أعلاه. |
| TASK-05: الترجمة البلغارية | نسخ نظام الويب + context + نسخ ملفات. |
| TASK-06: Quick Replies | UI components + ربط مع MessagingService. |
| TASK-07: Price Alerts | Cloud Function + Push notification. |
| TASK-08: Reviews | نقل components من الويب + تعديل. |
| TASK-10: Onboarding | UI + AsyncStorage. |
| TASK-12: VIN Check | API integration بسيط. |

### 🔴 يحتاج نموذج قوي (Opus / o1-pro) أو مطور بشري

| المهمة | السبب |
|---|---|
| TASK-09: AI Price Estimator | statistical modeling + data aggregation + قرارات معمارية |
| TASK-13: SEO Pages | SSR/SSG strategy + routing + محتوى بلغاري |
| TASK-14: Smart Sell from Photo | ربط Gemini API + Camera + Form autofill |
| TASK-15: CRA → Vite | يكسر البناء بسهولة. يحتاج فهم plugins + env vars + overrides |
| TASK-16: Dealer Dashboard | نظام اشتراكات + دفع + analytics. معمارية كاملة. |
| TASK-17: Financing Calculator | نمذجة مالية أو API بنوك حقيقية |

---

## القسم 5: الأوامر السريعة (Quick Reference)

```bash
# تشغيل الموبايل
cd mobile_new && npx expo start

# تشغيل الويب
cd web && npm start

# فحص Types (موبايل)
cd mobile_new && npx tsc --noEmit

# فحص Types (ويب)
cd web && npm run type-check

# بناء الويب
cd web && npm run build

# البحث عن console.log المتبقية
cd mobile_new && grep -rn "console\.\(log\|error\|warn\)" src/services/ --include="*.ts" --include="*.tsx"

# البحث عن مفاتيح Algolia
grep -r "ALGOLIA" web/.env* web/src/config/ --include="*.ts" --include="*.env*"
```

---

## القسم 6: الخلاصة الاستراتيجية

| التقييم | النسبة | التفصيل |
|---|---|---|
| **الويب** | 75% منتج حقيقي | Features ممتازة متقدمة على المنافسين. مثقل بكود ميت + CRA deprecated. |
| **الموبايل** | 30% واجهة عرض | يبدو جميلاً لكنه shell فارغ. إطلاقه بهذه الحالة = ضرر بالسمعة. |

### الفرق بينك وبين mobile.bg

هم يملكون **بيانات + ثقة مبنية على 15 سنة**.
أنت تملك **تقنية أفضل** لكن ليس لديك سبب مقنع يخلي البلغاري يتحول إليك.

### الميزات القاتلة (بعد إصلاح P0)

1. **AI Price Estimator** — "السعر العادل: €22,000 - €26,000" — لا أحد في بلغاريا يقدمها
2. **Price Drop Alerts** — "سعر BMW X5 نزل!" — سبب لتثبيت التطبيق وعدم حذفه
3. **VIN Check** — "هذه السيارة نظيفة" — عنصر ثقة ضخم

### خطة البدء

**ابدأ فوراً بـ TASK-01 → TASK-02 → TASK-03** (3 مهام سهلة = أثر فوري خلال يوم واحد)
**ثم TASK-04 → TASK-05** (المهمتان الأهم = بحث حقيقي + ترجمة بلغارية)
**ثم الباقي بالترتيب.**
